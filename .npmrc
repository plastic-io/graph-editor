# @plastic-io packages live on npmjs, whatever a developer's default registry is.
# Without this, a machine pointed at a private registry (CodeArtifact, GitHub
# Packages) resolves them there and gets a 404 that reads like "no such package".
@plastic-io:registry=https://registry.npmjs.org/
