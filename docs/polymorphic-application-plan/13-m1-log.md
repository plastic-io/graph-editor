# 13. M1 log

| Date | Item | Status | Evidence |
|---|---|---|---|
| 2026-09-20 | PB-010 JWT verification (jose 5, RS256, issuer+audience bound, JWKS cached per Lambda) | done | `graph-server/src/auth/jwt.ts`, tests `src/__tests__/auth.js` |
| 2026-09-20 | PB-011 REST Lambda REQUEST authorizer on every non-OPTIONS method | done, deployed to dev | `serverless.yaml` `custom.jwtAuthorizer`; live: `GET /toc.json` without token → 401, bad token → 401, OPTIONS → 200 |
| 2026-09-20 | PB-012 WebSocket `$connect` authorizer (token as `Sec-WebSocket-Protocol: access_token, <jwt>`), principal stored on the connection record, every later message attributed through `withPrincipal` in `handler.ts`; client-supplied identity fields ignored | done, deployed | `src/auth/principal.ts`, `broadcastService.connect`; live: handshake without/with bad token → 401. **Positive path (real token, subprotocol echo through API Gateway) pending the browser test.** |
| 2026-09-20 | PB-013 policy module | skeleton | `src/policy/decide.ts` (reference-instance owner policy, OWNER_SUBS, agent scopes); the matrix arrives with admission |
| 2026-09-20 | PB-014 editor sends the token (audience = server URL, mandatory login in server mode, subprotocol handshake, bearer on CRDT HTTP routes, refresh before reconnect with backoff) | done, pushed | `graph-editor` commit 4df455d; ratchet 505 = baseline; tests 61 + 16 pass; build ok |
| 2026-09-20 | PB-015 route hardening | done earlier (task 1); the interim API key is now removed in favour of the token | `serverless.yaml` |
| 2026-09-20 | Decision | Auth0 API created by the owner with identifier `https://rmeu8g85f4.execute-api.us-west-1.amazonaws.com/dev`; scopes/M2M/organizations deferred (single principal) | §12.2 |
| 2026-09-21 | Browser test of PB-014 (local mode) | passed | Editor pointed at the local dev server (`http://localhost:3030`, no authorizer) rendered the manager, fetched `toc.json` (200), opened the socket without a token and created a graph. First failure explained: the audience was derived from the local URL and Auth0 rejected the exchange ("Service not found: http://localhost:3030"); the exception escaped the router guard, and a reload on the callback URL then produced "Invalid state" (no stored transaction). Fixed in editor commits b09cfa1 (callback never fatal, exchange only with a fresh code/state, one auto-login per tab) and 3f1ddc9 (audience only from an HTTPS server URL or an explicit setting). |
| 2026-09-21 | Browser test of PB-014 against the AWS dev stage (real token, subprotocol echo) | **passed** | Verified in Chrome with the owner's session: login round-trip (Auth0 consent once, since the redirect target is localhost), `GET /toc.json` → 200 with the bearer header (117 entries rendered), `GET /crdt/{id}/state` → 200, WebSocket to `wss://8tx9g59h95…/dev` open with `protocol: "access_token"` (API Gateway passes the `$connect` subprotocol echo), subscriptions `graph-crdt-{id}` and `graph-notify-{id}` active, graph "MIDI Proving Ground" (3 nodes) loaded from S3, no auth/sync console errors. |
| 2026-09-21 | Audience decision | done | The tenant's authorization endpoint could not resolve the URL-shaped identifier created for this, but resolves the pre-existing API `plastic-io-graph-server`; the server now expects that audience (`AUTH0_AUDIENCE`) and publishes it at `GET /.well-known/oauth-protected-resource` (RFC 9728, public), from which the editor discovers it (`resolveAudience`). The unused `plastic-io-api` entry in Auth0 can be deleted. |
| 2026-09-21 | Fixes found by the browser test | done | editor: callback exchange never fatal and only on a fresh code/state (b09cfa1); audience only for HTTPS servers, local dev server needs no token (3f1ddc9); metadata-driven audience (0e39e95); one socket at a time, send() queues unless open, Auth0 cache in localStorage + refresh tokens so reloads keep the session, bearer header on toc.json/artifact fetches via `authorizedFetch` (cd6fe11). server: metadata route + audience default (m1-audience branch, deployed). |

| 2026-09-21 | PB-020 `MutationEnvelope` v2 + acks, server half | done, deployed | `graph-server/src/admission/{envelope,admit}.ts`; every WS `yjs` sync frame and `POST /crdt/{id}/update` passes `AdmissionService.admit()`; the sender gets `{kind:'ack'|'reject', mutationId, decision, code, reason, updateId}`; a legacy frame without `mutationId` gets one minted (`legacy:true`); idempotent replay on the same id + bytes, refusal on the same id + different bytes; the poisoning frame from the discovery record is rejected before anything is stored; server commit f969638 (master) |
| 2026-09-21 | PB-020 editor half + PB-110 SyncStatus panel | done, pushed | editor commit c5a0e30: `WssCrdtProvider` sends `schemaVersion:2`, a ULID `mutationId` and `clientInfo` on the socket and the HTTP fallback; `packages/SyncStatus` (top-bar cloud pill: yellow while a change is unanswered, red when one was rejected, menu lists each change with the server's code/reason) |
| 2026-09-21 | PB-021 staged validation + semantic diff | done, deployed | shared engine in `packages/GraphCrdt/{namespaces,diff}.ts` (`semanticDiff(before, after)` → `DiffSummary`: namespaces, per-node ops in the §5.1 vocabulary, connectors, privilege delta; 9 specs); server `src/admission/staging.ts` applies the update to a copy of the head (`CrdtStore.loadMerged`), refuses `STALE_BASE` when the update depends on structs the server never saw, `SCHEMA_INVALID` when it would empty the graph or raise the schema version past `SCHEMA_VERSION`; `meta` is read off the documents since `toJSON` omits it; the compact diff travels in the ack and the audit record; server commit d16fee4 |
| 2026-09-21 | PB-022 policy on the diff | done, deployed | `src/policy/decide.ts`: `requiredAuthorities(diff)` (commit always; `graph:connect-privileged` for placement/capabilities changes or wiring into a privileged node; `iac:propose` for the iac namespace; `iac:approve` for `aws:*`/`iam*` capabilities); `serverOwnedViolation` refuses `meta`/`observed*`/`policy` for every non-system principal after the seed; policy version `m1-diff`; tests: agent scoped to `graph:commit` cannot mark a node `placement:'server'`, owner can; a forged `observedStatus` root key is refused for the owner too |
| 2026-09-21 | PB-023 audit chain | done, deployed | `src/audit/chain.ts`: `audit/<graphId>/<ulid>.json` records with `seq`, `prev` (previous hash) and `hash` (sha256 of canonical JSON), `HEAD.json` pointer, monotonic ids; `verify()` walks by `seq` and reports content/prev/seq/HEAD breaks (test tampers with a record). **Known gap:** two admissions of one graph overlapping in different Lambda instances can read the same HEAD and fork the chain; `verify` reports it as a break; the fix is a conditional `If-Match` write of HEAD (S3Service does not pass conditions today) |
| 2026-09-21 | PB-025 limits | done, deployed | `src/admission/limits.ts`: `MAX_UPDATE_BYTES` (1 MiB), `MAX_STRUCTS` (200k), `RATE_MAX_MUTATIONS`/`RATE_MAX_REJECTIONS` per minute per principal (in-memory per Lambda instance, a brake not an accounting system), all `serverless.yaml` environment settings; the rate check runs before decoding so garbage costs nothing to refuse |
| 2026-09-21 | PB-024 reject recovery + reconnect | done, pushed | editor commit 1319fad. **One mutation in flight per graph** (D-13): the server handles frames concurrently, so a second update could arrive before the one it builds on and be refused `STALE_BASE`; everything edited while waiting merges into the next envelope. Unanswered after 20 s → resent under the same id (up to 5 times; the server replays its answer). `WSSDataProvider.onOpen` → after a reconnect the provider exchanges state vectors again and resends the unanswered mutation. On `reject`: `GraphStore.reloadFromServer()` closes the graph, clears the IndexedDB copy (`IndexedDBCrdtProvider.remove`, otherwise the refused structs would be resent on the next open) and opens it again from the server; the graph as it stood and the names of the lost changes are kept in the SyncStatus menu as a downloadable JSON draft (S-2 resolved for M1 by quarantine, D-14); `Editor.vue` rebinds Monaco when its `ytext` is swapped. Integration test: reload after a rejected change clears the browser copy and re-opens the server's state |
| 2026-09-21 | Server test count | 114 pass | `npx jest` in graph-server (envelope, stage A, transports, stage B: layout diff, agent scope denial, server-owned namespace, STALE_BASE then accept, empty-graph refusal, audit chain + tamper, rate limit) |
| 2026-09-21 | Editor checks | green | ratchet 505 = baseline, `test:crdt` 70, `test:integration` 17, `build-only` ok |
| 2026-09-21 | Browser verification of PB-020/024 against the AWS stage | **passed** (after two fixes) | Chrome, owner session, graph "MIDI Proving Ground". Ack path: a node move (layout only) was tracked as pending and answered by the server with `decision:'accepted'` and an `updateId`; the pill read "All changes accepted by the server"; S3 `audit/<g>/` holds the chain (seq 1..3, `prev` links, HEAD.json = newest hash) and the accepted record carries `diff.namespaces = ["layout"]`, principal `google-oauth2|…`, `policyVersion m1-diff`. Reject path: a garbage frame sent through the provider was answered `SCHEMA_INVALID: not a Yjs V2 update`, the console logged the rejection and "Reloading graph … from the server", the graph re-opened on a new session with the server's state, the quarantine held the lost change's name and the graph JSON, and the pill read "1 change(s) rejected by the server". **Bugs the test found (editor commit 23f7a83):** (1) `WssCrdtProvider` imported the SyncStatus store through the package entry point, which pulls in the orchestrator: a circular import left the helper undefined while `connect()` ran, so the update listener was never attached and nothing was ever sent; leaf-module imports fix it. (2) The store defined getters and actions with the same names (`accepted`, `rejected`); Pinia exposes both on one object and the getter wins, so the first ack threw; the actions are `markAccepted`/`markRejected`. Both were invisible to the unit and integration suites because neither runs the plugin graph or Pinia's getter/action merge on this store: a jsdom smoke test that boots `src/main.ts`'s plugin list is worth adding (PB-118 neighbour). |

Next in M1: PB-060/061 (scheduler 2.1: `ExecutionHandle`, completion, cancellation, budgets, `host` binding; then the owner publishes `@plastic-io/plastic-io` 2.1.0 by hand) → M1 exit review.
