# 13. M1 log

| Date | Item | Status | Evidence |
|---|---|---|---|
| 2026-09-20 | PB-010 JWT verification (jose 5, RS256, issuer+audience bound, JWKS cached per Lambda) | done | `graph-server/src/auth/jwt.ts`, tests `src/__tests__/auth.js` |
| 2026-09-20 | PB-011 REST Lambda REQUEST authorizer on every non-OPTIONS method | done, deployed to dev | `serverless.yaml` `custom.jwtAuthorizer`; live: `GET /toc.json` without token → 401, bad token → 401, OPTIONS → 200 |
| 2026-09-20 | PB-012 WebSocket `$connect` authorizer (token as `Sec-WebSocket-Protocol: access_token, <jwt>`), principal stored on the connection record, every later message attributed through `withPrincipal` in `handler.ts`; client-supplied identity fields ignored | done, deployed | `src/auth/principal.ts`, `broadcastService.connect`; live: handshake without/with bad token → 401. **Positive path (real token, subprotocol echo through API Gateway) pending the browser test.** |
| 2026-09-20 | PB-013 policy module | skeleton | `src/policy/decide.ts` (reference-instance owner policy, OWNER_SUBS, agent scopes); the matrix arrives with admission |
| 2026-09-20 | PB-014 editor sends the token (audience = server URL, mandatory login in server mode, subprotocol handshake, bearer on CRDT HTTP routes, refresh before reconnect with backoff) | done, pushed | `graph-editor` commit 4df455d; ratchet 505 = baseline; tests 61 + 16 pass; build ok |
| 2026-09-20 | PB-015 route hardening | done earlier (task 1); the interim API key is now removed in favour of the token | `serverless.yaml` |
| 2026-09-20 | Decision | Auth0 API created by the owner with identifier `https://rmeu8g85f4.execute-api.us-west-1.amazonaws.com/dev`; scopes/M2M/organizations deferred (single principal) | §12.2 |
| 2026-09-21 | Browser test of PB-014 (local mode) | passed | Editor pointed at the local dev server (`http://localhost:3030`, no authorizer) rendered the manager, fetched `toc.json` (200), opened the socket without a token and created a graph. First failure explained: the audience was derived from the local URL and Auth0 rejected the exchange ("Service not found: http://localhost:3030"); the exception escaped the router guard, and a reload on the callback URL then produced "Invalid state" (no stored transaction). Fixed in editor commits b09cfa1 (callback never fatal, exchange only with a fresh code/state, one auto-login per tab) and 3f1ddc9 (audience only from an HTTPS server URL or an explicit setting). |
| 2026-09-21 | Browser test of PB-014 against the AWS dev stage (real token, subprotocol echo) | **passed** | Verified in Chrome with the owner's session: login round-trip (Auth0 consent once, since the redirect target is localhost), `GET /toc.json` → 200 with the bearer header (117 entries rendered), `GET /crdt/{id}/state` → 200, WebSocket to `wss://8tx9g59h95…/dev` open with `protocol: "access_token"` (API Gateway passes the `$connect` subprotocol echo), subscriptions `graph-crdt-{id}` and `graph-notify-{id}` active, graph "MIDI Proving Ground" (3 nodes) loaded from S3, no auth/sync console errors. |
| 2026-09-21 | Audience decision | done | The tenant's authorization endpoint could not resolve the URL-shaped identifier created for this, but resolves the pre-existing API `plastic-io-graph-server`; the server now expects that audience (`AUTH0_AUDIENCE`) and publishes it at `GET /.well-known/oauth-protected-resource` (RFC 9728, public), from which the editor discovers it (`resolveAudience`). The unused `plastic-io-api` entry in Auth0 can be deleted. |
| 2026-09-21 | Fixes found by the browser test | done | editor: callback exchange never fatal and only on a fresh code/state (b09cfa1); audience only for HTTPS servers, local dev server needs no token (3f1ddc9); metadata-driven audience (0e39e95); one socket at a time, send() queues unless open, Auth0 cache in localStorage + refresh tokens so reloads keep the session, bearer header on toc.json/artifact fetches via `authorizedFetch` (cd6fe11). server: metadata route + audience default (m1-audience branch, deployed). |

| 2026-09-21 | PB-020 `MutationEnvelope` v2 + acks, server half | done, deployed | `graph-server/src/admission/{envelope,admit}.ts`; every WS `yjs` sync frame and `POST /crdt/{id}/update` passes `AdmissionService.admit()`; the sender gets `{kind:'ack'|'reject', mutationId, decision, code, reason, updateId}`; a legacy frame without `mutationId` gets one minted (`legacy:true`); idempotent replay on the same id + bytes, refusal on the same id + different bytes; the poisoning frame from the discovery record is rejected before anything is stored; server commit f969638 (master) |
| 2026-09-21 | PB-020 editor half + PB-110 SyncStatus panel | done, pushed | editor commit c5a0e30: `WssCrdtProvider` sends `schemaVersion:2`, a ULID `mutationId` and `clientInfo` on the socket and the HTTP fallback; `packages/SyncStatus` (top-bar cloud pill: yellow while a change is unanswered, red when one was rejected, menu lists each change with the server's code/reason) |
| 2026-09-21 | PB-021 staged validation + semantic diff | done, deployed | shared engine in `packages/GraphCrdt/{namespaces,diff}.ts` (`semanticDiff(before, after)` → `DiffSummary`: namespaces, per-node ops in the §5.1 vocabulary, connectors, privilege delta; 9 specs); server `src/admission/staging.ts` applies the update to a copy of the head (`CrdtStore.loadMerged`), refuses `STALE_BASE` when the update depends on structs the server never saw, `SCHEMA_INVALID` when it would empty the graph or raise the schema version past `SCHEMA_VERSION`; `meta` is read off the documents since `toJSON` omits it; the compact diff travels in the ack and the audit record; server commit d16fee4 |
| 2026-09-21 | PB-022 policy on the diff | done, deployed | `src/policy/decide.ts`: `requiredAuthorities(diff)` (commit always; `graph:connect-privileged` for placement/capabilities changes or wiring into a privileged node; `iac:propose` for the iac namespace; `iac:approve` for `aws:*`/`iam*` capabilities); `serverOwnedViolation` refuses `meta`/`observed*`/`policy` for every non-system principal after the seed; policy version `m1-diff`; tests: agent scoped to `graph:commit` cannot mark a node `placement:'server'`, owner can; a forged `observedStatus` root key is refused for the owner too |
| 2026-09-21 | PB-023 audit chain | done, deployed | `src/audit/chain.ts`: `audit/<graphId>/<ulid>.json` records with `seq`, `prev` (previous hash) and `hash` (sha256 of canonical JSON), `HEAD.json` pointer, monotonic ids; `verify()` walks by `seq` and reports content/prev/seq/HEAD breaks (test tampers with a record). **Known gap:** two admissions of one graph overlapping in different Lambda instances can read the same HEAD and fork the chain; `verify` reports it as a break; the fix is a conditional `If-Match` write of HEAD (S3Service does not pass conditions today) |
| 2026-09-21 | PB-025 limits | done, deployed | `src/admission/limits.ts`: `MAX_UPDATE_BYTES` (1 MiB), `MAX_STRUCTS` (200k), `RATE_MAX_MUTATIONS`/`RATE_MAX_REJECTIONS` per minute per principal (in-memory per Lambda instance, a brake not an accounting system), all `serverless.yaml` environment settings; the rate check runs before decoding so garbage costs nothing to refuse |
| 2026-09-21 | PB-024 reject recovery + reconnect | done, pushed | editor commit 1319fad. **One mutation in flight per graph** (D-13): the server handles frames concurrently, so a second update could arrive before the one it builds on and be refused `STALE_BASE`; everything edited while waiting merges into the next envelope. Unanswered after 20 s → resent under the same id (up to 5 times; the server replays its answer). `WSSDataProvider.onOpen` → after a reconnect the provider exchanges state vectors again and resends the unanswered mutation. On `reject`: `GraphStore.reloadFromServer()` closes the graph, clears the IndexedDB copy (`IndexedDBCrdtProvider.remove`, otherwise the refused structs would be resent on the next open) and opens it again from the server; the graph as it stood and the names of the lost changes are kept in the SyncStatus menu as a downloadable JSON draft (S-2 resolved for M1 by quarantine, D-14); `Editor.vue` rebinds Monaco when its `ytext` is swapped. Integration test: reload after a rejected change clears the browser copy and re-opens the server's state |
| 2026-09-21 | Server test count | 114 pass | `npx jest` in graph-server (envelope, stage A, transports, stage B: layout diff, agent scope denial, server-owned namespace, STALE_BASE then accept, empty-graph refusal, audit chain + tamper, rate limit) |
| 2026-09-21 | Editor checks | green | ratchet 505 = baseline, `test:crdt` 70, `test:integration` 17, `build-only` ok |
| 2026-09-21 | Browser verification of PB-020/024 against the AWS stage | **passed** (after two fixes) | Chrome, owner session, graph "MIDI Proving Ground". Ack path: a node move (layout only) was tracked as pending and answered by the server with `decision:'accepted'` and an `updateId`; the pill read "All changes accepted by the server"; S3 `audit/<g>/` holds the chain (seq 1..3, `prev` links, HEAD.json = newest hash) and the accepted record carries `diff.namespaces = ["layout"]`, principal `google-oauth2|…`, `policyVersion m1-diff`. Reject path: a garbage frame sent through the provider was answered `SCHEMA_INVALID: not a Yjs V2 update`, the console logged the rejection and "Reloading graph … from the server", the graph re-opened on a new session with the server's state, the quarantine held the lost change's name and the graph JSON, and the pill read "1 change(s) rejected by the server". **Bugs the test found (editor commit 23f7a83):** (1) `WssCrdtProvider` imported the SyncStatus store through the package entry point, which pulls in the orchestrator: a circular import left the helper undefined while `connect()` ran, so the update listener was never attached and nothing was ever sent; leaf-module imports fix it. (2) The store defined getters and actions with the same names (`accepted`, `rejected`); Pinia exposes both on one object and the getter wins, so the first ack threw; the actions are `markAccepted`/`markRejected`. Both were invisible to the unit and integration suites because neither runs the plugin graph or Pinia's getter/action merge on this store: a jsdom smoke test that boots `src/main.ts`'s plugin list is worth adding (PB-118 neighbour). |
| 2026-09-21 | Diff refinement from the browser evidence | done | The accepted move's audit record read `namespaces: ["definition","layout"]` because the editor bumps `version` and `properties.lastUpdate` on every commit. Those keys are now the `housekeeping` namespace (op `touch`), so a pure move is `["housekeeping","layout"]` and policy on `definition` means what it says. `packages/GraphCrdt/namespaces.ts`; 71 CRDT specs, 114 server tests. |
| 2026-09-21 | PB-030/031/032/033/037 revisions, grown from the document's own history (owner: "find some way to organically version the graph using the yjs lib") | done, deployed | A revision is a **Yjs snapshot** of the graph's document (`Y.snapshot` → `encodeSnapshotV2`: state vector + delete set, a few hundred bytes). `graph-server/src/revisions/service.ts`: `cut` writes an immutable manifest (`revisions/<g>/<rev>.json`: seq, parent, label, snapshot, stateVector, headUpdateId, digests {definition, layout, full} over `graph-crdt/digest.ts` views, schemaVersion, pins {components, artifacts, runtime.scheduler}, mutationIds since the parent from the audit chain, counts, diffFromParent), the frozen projection, `HEAD.json`, an audit record, and **stamps `meta.revision` into the document as the system principal through the admission gate** (fanned out, so every replica knows its version; a client writing `meta` is refused, staging now compares the whole meta map). Cutting with nothing changed returns the head instead of a duplicate. `materialize` rebuilds any revision with `Y.createDocFromSnapshot` on a `gc:false` doc built from the merged log and verifies the digest; the test proves it after later edits, a node deletion and an S3 log snapshot (merged updates keep deleted content). `restore` reconciles the live head to the revision's projection and admits the result as an ordinary change (audited, merges, undoable). `activate` writes `active/<g>.json` and the execution projection with the revision id in its metadata; `writeProjections` no longer overwrites the endpoint file once a revision is active (execution follows activation, not live edits, §4.7.4). Routes `GET/POST /crdt/{id}/revisions`, `GET .../{rev}[?materialize=1]`, `POST .../{rev}/activate`, `POST .../{rev}/restore`; authorities `graph:commit` / `graph:activate`; system principals bypass `OWNER_SUBS`. 7 tests (121 total). Editor: provider calls, `packages/Graph/versions.ts` actions, `packages/Versions` bottom-bar menu (save with a name, preview without touching the document, restore, activate, current version chip from `meta.revision`). Server a3aa505, editor 5f1a55c. **Not yet:** HEAD CAS (S3Service passes no conditions), proposals (PB-035/036), log compaction (PB-038). |
| 2026-09-21 | PB-060 scheduler 2.1 | done, on branch `scheduler-2.1` of plastic-io/plastic-io (473a98a, pushed; **npm publish pending, owner does it by hand**) | `src/Execution.ts` (`Execution` = handle + scope: tracks every promise the scheduler starts, `arm()` after the synchronous prefix, `end` once at settle; budgets wallMs/hops/fanOut/depth/observations with one `BUDGET_EXCEEDED` error then cancel; `graceMs` → `abandoned`), `src/Cancellation.ts` + `src/ExecutionCancelled.ts` (token with AbortSignal, checked at every hop and before every delivery), `Scheduler.invoke()` → `ExecutionHandle`, `url()` = `invoke().done` (never rejects), `cancelAll()`, `beginExecution()` for emissions after an execution ended (`trigger:'edge'`, `parentExecutionId`); events stamped `seq`, `executionId`, `spanId`, `parentSpanId`; `endedge` after the set function (2.0.1 order), `afterSet` carries `err`; `host` binding (identity, `cancelled`, `signal`, `throwIfCancelled`, `emit` → `observation` event, `now`, `random`, embedder extensions); `compile: 'direct'` (source as written to the real `AsyncFunction`) with meriyah behind an option, compiled cache per scheduler; `onInput`/`onOutput` contract hooks (`warn`/`reject`); cross-graph connector fix (RT-13). **Discovery correction (RT-05):** top-level `await` in node code was blocked not only by meriyah 1.9 but by the ES5 build turning `async function(){}` into a generator helper whose constructor is plain `Function`; 2.1 obtains `AsyncFunction` at runtime. Tests: 27 legacy (one ordering expectation updated) + 14 new = 41, tslint clean. README section added. |
| 2026-09-21 | PB-061 both consumers | code adapted and pushed; **pins still 2.0.3 (editor) / 2.0.1 (server) until 2.1.0 is on npm** | Verified against the packed 2.1.0 tarball installed with `--no-save`: editor ratchet 505, 17 integration specs, production build (editor commit ff1045d `schedulerWorker.ts`: `cancelAll` on graph change, relays `observation`/`cancel`, accepts `cancel`); server 121 tests, no type errors (server commit 3adf0e4 `graphService.ts`: budget `{wallMs: graphTimeout, hops, fanOut, depth}`, `postGraph` logs `{executionId, state, reason, hops, errors}`). After publish: bump both pins to `^2.1.0`, redeploy the server, browser-check the editor. |
| 2026-09-21 | Scheduler 2.1 merged and **staged on npm** (awaiting the owner's approval on npmjs.com) | done | `scheduler-2.1` merged into master (plastic-io/plastic-io be9eb3d). Publishing is now npm **trusted publishing + staged publishing**: `continuous-integration-workflow.yml` (the filename registered as the trusted publisher on npmjs.com) runs test → `publish` job with `id-token: write`, Node 22, `npm install -g npm@latest` (staged publishing needs npm ≥ 11.15), `npm stage publish --access public`, skipping versions already on the registry; no token in the repo; the legacy Node 10 token job is gone. Two dead ends on the way: `setup-node`'s `registry-url` writes an empty `_authToken` + `always-auth` into `.npmrc` and npm uses that instead of OIDC (E401); a publish job in any other workflow file gets `ENEEDAUTH` because the OIDC exchange is matched by filename. Also: lockfile regenerated (npm 10's `npm ci` is stricter), `npm pkg fix` normalized the scripts, CI on Node 22 (run 35621258799 green). After approval: bump both pins to `^2.1.0`, redeploy the server, browser-check. |
| 2026-09-21 | 2.1.0 approved and live; PB-061 pins moved; browser check of Versions and the scheduler | **passed** | `@plastic-io/plastic-io@2.1.0` is `latest` on npm (approved by the owner). Editor dc86b37 and server 8efae0d pin `^2.1.0`; editor ratchet 505, 17 integration specs, build; server 121 tests, no type errors; dev stage redeployed. Chrome, owner session, graph "MIDI Proving Ground" on scheduler 2.1.0 (Vite serves 2.1.0; only the graph's own pre-existing Web MIDI template exception in the console): **Save** "browser check v1" → "Saved version 1", the list shows v1, the document carries `meta.revision {seq:1, label}` delivered by the server's stamp over the socket, chip "now: v1"; a node move (accepted) → **Preview** v1 shows the original position with the live graph untouched (`inRewindMode`), **Back to live** returns the moved position; **Restore** v1 → the server's admitted "Restore version 1" update arrives through fan-out and both the view and the document are back at the original position; **Activate** → "Version 1 is now what the server runs", green marker. S3: `revisions/<g>/{manifest, projection, HEAD}`, `active/<g>.json` (seq 1, by the owner, digest), audit chain seq 1–8: `mutation.accepted Sync`, `Move node back`, `mutation.rejected Garbage frame`, `revision.cut browser check v1`, `mutation.accepted Version 1` (system:revisions), `Move node (versions check)`, `Restore version 1`, `revision.activated`. |
| 2026-09-21 | Publishing integrated with revisions (owner: "the publishing and importing of published artifacts will work differently") | done, deployed, browser-verified | **A published version is a revision.** Server `src/components/service.ts`: `publish(graphId, principal, {nodeId?, label?, revisionId?})` names a revision (cuts one if the graph changed, or uses an older one by id) and writes an immutable `components/<publishedId>/<seq>/{manifest,artifact}.json` + `HEAD.json`; `version` = the revision's `seq`, republishing an unchanged graph returns the existing version. Manifest per §4.3.2: contract (external ports for graphs, ports for nodes), capabilities, placement, dependencies (pins + legacy `artifacts/<id>.<v>` refs), provenance {principal, graph, revisionId, seq, mutationId}, `compat.runtime.ts` (2.1.0), three digests: `digest` over the **component view** (`graph-crdt/digest.ts componentView`: what a consumer's embedded copy must still equal, tolerant of what importers do to copies: dropped `url`/`artifact`, own id/version/graphId, layout, cleared connectors, `visible` fill-in), `artifactDigest`, `revisionDigest`. The 2.0 layout is still written (`graphs/projections/published/artifacts/<id>.<seq>.json`, endpoints file) and `GET /artifacts/{id}/{version}` serves the component first, then legacy; `GET /graph/{id}/{version}` resolves a number to the revision (`revisions/<g>/seq/<n>.json` index). New routes `POST /crdt/{id}/publish`, `GET /components/{id}`, `GET /components/{id}/{version}`; WS `publishGraph`/`publishNode` delegate and answer with version + revision. **Integrity at admission:** every pinned node a change added, re-pinned, or whose embedded copy/code/ports it touched is checked against its manifest; drift is recorded as `warnings` on the ack and in the audit (`COMPONENT_INTEGRITY=reject` refuses with `INTEGRITY_FAILURE`). TOC entries carry `revision-id` and `digest`. 7 tests (128 total). Editor: publish from the graph panel, the node panel (never reached the server in 2.0, GE-50) and per version in the Versions menu (published marker); imports fetch the manifest with the artifact and pin the node (`properties.component {publishedId, version, digest}`, `linkedGraph.revisionId`); the node panel shows the pin, finds newer versions and **upgrades in place** (`Graph/versions.ts upgradeComponent`: embedded copy, ports, edges follow; connectors kept on surviving outputs, dropped ones reported); two 2.0 paths used `artifacts/<id>.<v>` which never routed. Type baseline lowered to 460. Chrome: MIDI graph published v1 (revision 1) then, after an accepted edit, v2 (revision 2; audit: `revision.cut`, `Version 2` stamp, `component.published`); imported v1 into "Thoughtless Coral" → node pinned to v1 with the revision id, accepted; `upgradeComponent(…, 2)` → pin v2, accepted; S3 `components/<g>/{1,2}/`, legacy mirrors and TOC entries present; cleanup node deleted. |

**M1 is complete** (identity, admission with staged validation and policy on the diff, audit chain, envelope/acks/reject recovery, revisions as Yjs snapshots, scheduler 2.1 on both consumers). Next: M1 exit review against §9 (W1–W5 done; W6 MCP read/propose and W7 containment PB-062..066 are M2/M3 work: isolated-vm on nodejs22.x per spike S-1, `host` as the only binding, watchdog + budgets in the Lambda owner thread), plus the known gaps: audit-chain HEAD race (conditional S3 writes), revision HEAD CAS, proposals (PB-035/036), log compaction (PB-038), PB-118 type errors. (scheduler 2.1: `ExecutionHandle`, completion, cancellation, budgets, `host` binding; then the owner publishes `@plastic-io/plastic-io` 2.1.0 by hand) → M1 exit review.

# M2 log

| Date | Item | Status | Evidence |
|---|---|---|---|
| 2026-09-21 | PB-026 agent ops materialisation | done | `packages/GraphCrdt/ops.ts` `applyOps(projection, ops)`: the §5.1 `MutationOp` vocabulary applied to a copy of the projection (add/remove node, set-node-code, set-node-props with the editor's port-rename semantics, connect/disconnect with deterministic connector ids, pins, capabilities, placement, budget, iac), first failure stops the batch; 6 specs. The server reconciles the live document into the result with the same `reconcile()` an editor uses, so a proposal's bytes are what an editor would have produced (§6.1 convergence). |
| 2026-09-21 | PB-080 MCP Lambda | done, deployed | `graph-server/src/mcp/{server,handler}.ts` on `@modelcontextprotocol/server` 2.0.0 (SDK v2; **runtime moved to nodejs22.x**, Q-5): `POST /mcp` behind the JWT authorizer; the event becomes a web-standard `Request`; `createMcpHandler` builds one `McpServer` per request bound to the principal passed through `authInfo.extra`; 2025-era (non-envelope) clients are served as JSON through a per-request `WebStandardStreamableHTTPServerTransport` (`legacy:'reject'` on the modern handler); Origin allowlist `MCP_ALLOWED_ORIGINS`; 401 with `WWW-Authenticate resource_metadata` when unauthenticated. Every tool result carries the §5.1 envelope (`requestId`, `principal` with `delegatedBy`, `graphId`, `baseRevision`/`resultRevision` as `rev_<ulid>`, `policyVersion`, `serverTime`, `truncated`); errors `{code, message, retry{retryable, afterMs?, rebaseTo?}, details?}`; unknown fields are refused by the zod schemas (`.strict()`); per-principal rate brake (60 reads / 10 writes per minute); every call audited as `mcp.tool.<name>` on the graph's chain. |
| 2026-09-21 | PB-081 read tools + resources | done | `graph.summary` (graph or node, `ComponentSummary` with contract/effects/deps/degree/pointers, `untrusted:["purpose"]`), `graph.expand` (BFS ordered by depth then id; depth ≤ 4, ≤ 200 nodes, ≤ 256 KiB; `truncated{byDepth,byCount,byBytes}`; cursor bound to the revision → `STALE_BASE` otherwise; code only with `inspect-internals`), `component.search` (TOC-backed), `observations.query` (**audit-backed in this release**: mutation/revision/component/proposal/mcp records as observations; the execution observation store is M3 PB-054). Resources `plastic://graphs`, `graph/{id}`, `graph/{id}/rev/{rev}`, `.../node/{nodeId}[?expand=code]`, `component/{id}`, `component/{id}/{version}`, `graph/{id}/history[?limit]`, `graph/{id}/diff/{a}/{b}` (PB-034), `graph/{id}/proposal/{pid}`. **Reads name their state**: HEAD, or a revision cut by the system principal when the live definition moved (`SummaryService.headOrCut`), so `resultRevision` is always a real revision and proposals can name it as base. `src/summary/service.ts`. |
| 2026-09-21 | PB-082 proposal.create / proposal.validate | done | `src/proposals/service.ts`: `baseRevision` must be HEAD (else `STALE_BASE` + `rebaseTo`); ops applied to the live projection, reconciled, staged through `AdmissionService.stageOnly` (structure, staging, server-owned namespaces) without appending; record `proposals/<g>/<pid>.json` with the validated update bytes, `proposalDigest` (`sha256:`), compact diff, impact (downstream nodes, privilege delta), `requiredDecisions` (`approve` for every agent proposal in M2, `privileged-connect` / `iac-approve` when the proposer lacks those authorities), state `validated`/`awaiting-review`, 7-day expiry, idempotent on `idempotencyKey`; `.projection.json` for the editor's preview; audit `proposal.created`; `graph-notify` event. `validate` re-stages; with `rebase` it re-applies on the new head and reports `CONFLICT` when a `set-node-code` target's code changed underneath. |
| 2026-09-21 | Human side of proposals (commit path) | done | REST `GET/POST /crdt/{id}/proposals`, `GET .../{pid}`, `POST .../validate[?rebase=1]`, `POST .../decide` (approve bound to the digest, never by the proposer; an approver holding `graph:connect-privileged`/`iac:approve` settles those decisions too; reject), `POST .../commit`: the committing human's `admission.admit` of the stored bytes (audited as their mutation with the proposal as intent), fan-out to replicas, a revision cut labelled with the proposal, state `committed` with `mutationId` + `resultRevision`; a moved head → `STALE_BASE` until validated with rebase; re-commit is idempotent. `GET /crdt/{id}/audit` for the activity view. |
| 2026-09-21 | PB-086 agent delegations | done | `src/policy/delegation.ts`: `policy/agents/<sub>/<graphId|_all>.json` `{agentSub, graphId, delegatedBy, scopes, expiresAt}` written only with `policy:admin` and only with scopes the delegator holds (`PUT/DELETE /policy/agents/{sub}/{graphId}`, `GET /policy/agents`); `resolve()` gives an agent principal `delegatedBy` and effective scopes = delegation ∩ token scopes (token scopes only narrow); no live record → no scopes; `decide()` now denies agents with no scopes (before, an agent token without scopes passed as an owner); admission resolves agents before policy (`resolvePrincipal` hook). Editor: Settings → Agents panel (`packages/AgentSettings`, `settings-panel` plugin). |
| 2026-09-21 | PB-111 / PB-112 editor | done | `packages/Proposals` bottom-bar menu: proposals of the open graph with state, proposer + delegator, rationale marked untrusted, diff and pending decisions; Preview (the proposal's projection patched in under rewind mode, touched nodes selected, added connections in `watchConnectors`, removed in `errorConnectors`), Back to live, re-validate with rebase, Commit (through admission as the signed-in user), Reject; refreshes on `graph-notify` `proposal` events; an "Agent activity" list from the audit chain (agent, proposal and MCP records). `packages/Graph/proposals.ts` actions; provider calls in `WssCrdtProvider`. Editor commit 14c1283. |
| 2026-09-21 | Tests | 135 server (MCP suite drives the real `@modelcontextprotocol/client` over `handler.serve` and the Lambda face; delegation suite), editor 75 CRDT specs, 17 integration, ratchet 460 | jest 26 needed a resolver that falls back to Node's `exports`-aware resolution (`src/__testHelpers__/resolver.js`), an environment that lends the fetch globals (`webEnvironment.js`) and `node:` shims for the SDK's core imports |
| 2026-09-21 | Deviations from the plan | recorded | (1) **Tasks extension (PB-084) deferred**: no M2 tool exceeds the request budget, so a task store would be dead code; it lands with `proposal.simulate` / `graph.invoke` (M3). (2) `subscriptions/listen` (PB-085, S-3) not started: clients poll `observations.query {since}` and the editor uses the WebSocket notify channel. (3) The SDK's `LATEST_PROTOCOL_VERSION` constant reads 2025-11-25 while the package serves the 2026-07-28 envelope era; both eras are served. (4) The plan's `revisionId` wire form `rev_<ulid>` and `digest` `sha256:<hex>` are applied at the MCP boundary only; stores keep bare ids. |
| 2026-09-21 | Browser verification of M2 against the AWS dev stage (nodejs22.x) | **passed** (one fix) | From the editor page with the owner's session (the token never left the browser): `POST /mcp` `tools/list` → the six tools; `graph.summary` → envelope with the human principal, `resultRevision rev_…` (the existing head, since nothing had changed), `policyVersion m1-diff`, summary of "MIDI Proving Ground" (3 nested nodes); `graph.expand` from a node, `direction both, depth 2` → 3 nodes, 2 edges, nothing truncated; `proposal.create` against that revision (`set-node-props` description) → `validated`, no decisions (a human proposer), diff namespace `definition`. In the editor: the Proposals badge showed 1 and the notify channel raised "An agent proposed …" without a refresh; the panel listed the proposal with state, proposer, base revision, the rationale marked untrusted and the diff; **Preview** entered rewind mode with the touched node selected and the proposed description in the view while the live document stayed unchanged; **Commit** admitted the stored bytes as the owner → the live document changed through fan-out, state `committed` with `mutationId` and `resultRevision`, the document's `meta.revision` moved to seq 3, the card read "Committed … as you". The change was reverted afterwards. Agents: `PUT/GET/DELETE /policy/agents/…` round-tripped from the page through the provider (delegation written with `delegatedBy` = the owner, listed, removed); the Settings panel lists the new "Agents" section (its expanded content was not captured in the automation run). **Fix found by the test (server 509b9e9)**: the event source service created the delegation store before `this.store` was assigned, so every delegation route returned an empty 500; the store now comes from the CRDT store and those routes log failures. Jest could not catch it because the tests construct the store directly. |

Next: M3 (log below).


# M3 log

Server master ec953be (6566663, 4a5f477, ec953be), deployed to the dev stage; 145 server tests.

| Date | Item | Status | Evidence / notes |
|---|---|---|---|
| 2026-09-21 | PB-050 capability types + resolver | done | `graph-server/src/runtime/capabilities.ts`: a capability is `kind[:scope,scope]` or `{kind, scope[], optional}` (`net:https`, `storage:kv`, `secret`, … ); scope patterns match hosts (`*.example.com` also matches the apex) and key prefixes (`ratelimit/*`); **effective = instance ∩ manifest ∩ principal**, evaluated in that order so the first refusing layer is named (`CapabilityDenied.layer` = instance \| manifest \| principal); a bare kind without a scope grants nothing; nesting never widens (a pinned component runs under its published manifest's list, a restricted principal under its own). |
| 2026-09-21 | PB-051 server capability host | done (ambient context kept, see deviations) | `src/runtime/host.ts` bound as scheduler 2.1's `host`: `host.fetch` (HTTPS only, host in the `net:https` scope, the execution's abort signal attached), `host.kv.get/put` (S3 `kv/<graphId>/<key>.json`, prefix scopes), `host.secret(ref).openai()` / `.header()` (the value never enters node scope; refs resolve through `SECRET_REFS` `ref=SecretsManagerName`), `host.emit(kind, data)` → `custom` observation, `host.capabilities`. Every call is an `effect` / `effect.denied` observation with kind, scope, decision and layer; privileged kinds (secret) are also appended to the audit chain with the `executionId`. |
| 2026-09-21 | PB-053 / PB-054 / PB-056 / PB-057 observations | done | `src/runtime/observe.ts` `ObservationRecorder`: kinds `exec.begin`, `edge.input`, `route`, `exec.error`, `effect`, `effect.denied`, `budget.exhausted`, `contract.violation`, `custom`, `exec.end`; each carries a monotonic ULID `id`, `seq`, `executionId`, `correlationId`, `graphId`, `revisionId`, `domain`, `owner`, `nodeId`/`edgeField`/`connectorId`, `spanId`. Payload capture follows the port: `capture: meta (default) \| full \| none`, `redaction: hash \| secret`; meta = `{type, bytes, hash}`; a full capture over ~8 KiB becomes `redacted:"size"`. Cap `MAX_OBSERVATIONS` (5000) or 2 MiB, then routes/inputs are sampled 1-in-10 under a `budget.exhausted` marker; errors, effects and the end always land. Sink: NDJSON `observations/<g>/<yyyymmddhh>/<executionId>.ndjson`, record `executions/<id>.json` + index `executions/by-graph/<g>/<id>.json`. Live: `observation` events on the graph's notify channel (errors, denials and violations always; the rest when the client asked for `observation` broadcasts). |
| 2026-09-21 | PB-045 contracts, server half | done | `src/runtime/contracts.ts`: ajv on scheduler 2.1's `onInput`; a port with `schema` is checked on delivery → `contract.violation` observation; `graph.properties.contractMode` `warn` (default, deliver) or `reject` (drop the hop). `assignable(from, to)` for the editor's connect-time check (PB-045 editor half pending). |
| 2026-09-21 | Executor wired to the HTTP route | done, deployed | `src/runtime/executor.ts` `ExecutionRunner.run()` builds scheduler 2.1 with the budget, host, contract hooks and recorder and returns an `ExecutionSummary` `{executionId, revisionId, graphId, state, duration, hops, errors, effects{allowed,denied}, observations{count,key,sampled,capped}, budget}`. `graphService.router` runs through it: execution id from `X-Execution-Id` or minted, revision from `active/<g>.json` else `live`, the caller recorded as owner, pinned components narrowed to their manifest; the 2.0 `this` context (openai/AWS/console/callback) and the legacy notify events are kept. **The HTTP response is the summary JSON** (`Content-Type: application/json`) unless the node answered through `this.callback` — the worker posts `{type:"summary"}` / `{type:"response"}` to the main thread, and `_defaultRoute` now returns what the router resolved (it always answered `"ok"` before, so 2.0 custom responses never reached the client over HTTP). |
| 2026-09-21 | MCP observations | done, deployed | `observations.query` reads real executions (one by `executionId`, or the newest of the graph) merged with the audit trail (audit kinds only when the filter names `mutation`/`revision`/`component`/`proposal`/`mcp` or names no kind), sorted by id with `since`/`cursor`; payload values need `graph:inspect-payloads`, otherwise `payload:{meta, redacted:"payload"}`. Resources `plastic://graph/{id}/executions` (newest 100, revision as `rev_…`) and `plastic://graph/{id}/execution/{executionId}` (record + observations). |
| 2026-09-21 | Fixes found live | done, deployed | (1) A proposal committed through MCP + REST changed the graph and cut a revision, but execution still ran the old code: `graphs/projections/endpoints/<url>.json` (what `graphService.init` loads) was only refreshed by editor edits (`crdtService`); `ProposalService.commit` and `RevisionService.restore` now call `crdtStore.writeProjections` (test in `mcp.js`). (2) `fetch` with an `X-Execution-Id` header failed CORS preflight on `/{proxy+}`; the route now lists `Content-Type, Authorization, X-Execution-Id, X-Correlation-Id`. |
| 2026-09-21 | Tests | 145 server | `src/__tests__/runtime.js` (9): parsing/scope matching, three-layer intersection with the refusing layer named, capture/redaction, end-to-end NDJSON + record with per-port redaction, host fetch/kv/secret allowed and denied with the audit chain verified, manifest and principal narrowing, contract warn/reject, cap + sampling + `budget.exhausted`, legacy events + `this` context; `mcp.js` +1: `observations.query` over a real execution, cursoring, both resources, agent without `inspect-payloads` sees meta only. |
| 2026-09-21 | Browser verification against the AWS dev stage | **passed** (two fixes above) | From the editor page with the owner's session (token stays in the browser). (a) "Cuddly Peacock" (a 2.0-era graph with no CRDT document, so `revisionId:"live"`): `POST /dev/CuddlyPeacock/MoldySeagull` → 200 JSON summary `state completed, hops 2, observations 5`; `observations.query {executionId}` → `exec.begin, edge.input, route, edge.input, exec.end` with meta-only payloads (`meta:object:5731`), the human principal in the envelope; `plastic://graph/{id}/executions` lists it; `.../execution/{id}` returns record + 5 observations; the unfiltered query interleaves `mcp.tool.observations.query` audit entries with execution observations; S3 holds the NDJSON and the record. (b) "Thoughtless Coral": `proposal.create` (MCP) with `set-capabilities ["net:https:www.githubstatus.com","storage:kv:demo/*"]` + `set-node-code` (host.fetch / denied fetch / kv put+get / denied kv / denied secret / emit) → `validated`, no decisions for the owner → `POST /crdt/{id}/proposals/{pid}/commit` → committed → `POST /crdt/{id}/publish` → version 2, manifest capabilities carried. After fix (1), a second proposal (description) committed and refreshed the projection; `POST /dev/ThoughtlessCoral/RichCrocodile` with `X-Execution-Id` → the same id back, `effects {allowed:3, denied:3}`, 10 observations: `effect net:https:www.githubstatus.com allowed`, `effect.denied net:https:example.org (instance)`, `effect storage:kv demo/last` ×2, `effect.denied storage:kv other/x`, `effect.denied secret:openai`, `custom checked {indicator:"none", denied:"CapabilityDenied:instance", …}`. S3: `kv/<g>/demo/last.json` stamped with the execution id, `executions/by-graph/<g>/` has both runs, the audit chain's last effect entry is `effect.denied secret openai` with the execution id, the projection carries the capabilities and the new code. |
| 2026-09-21 | Deviations from the plan | recorded | (1) The 2.0 ambient context (`AWS`, `openai`, `require`, `this.callback`) is **still bound** for existing graphs; it goes when the isolated-vm executor (PB-062/063) replaces the worker thread — until then `host` is the documented API and effects through it are the only observed ones. (2) PB-055 subscriptions: live observations ride the existing notify channel (already authorized per connection); no separate subscription/redaction filter yet. (3) PB-057 retention lifecycle rules not configured on the bucket. (4) `graph.invoke` / Tasks (PB-084) not started: the HTTP route is the invoke path. (5) Browser `host` (PB-052) and the editor connect-time assignability (PB-045 FE) pending. |

| 2026-09-21 | PB-052 browser `host` + PB-053 both domains + PB-064 browser budgets | done, deployed, browser-verified | The capability model, the observation recorder and the host binding moved into `@plastic-io/graph-crdt` (`packages/GraphCrdt/{capabilities,observe,host,contracts,ulid}.ts`), which both repositories already link, so a grant and an observation mean the same thing in either domain; the server's `src/runtime/{capabilities,observe,host}.ts` are now import paths that add only what is theirs (the OpenAI client built from a secret the node never sees), and `ObservationRecorder.handle(name, event)` was split out of `attach` because the browser keeps one scheduler for the open graph and routes events to the recorder of the execution they belong to. Editor: `packages/Orchestrator/browserExecutions.ts` (recorder per execution, `host` per node invocation), `schedulerWorker` gets budgets (`wallMs 120000, hops 100000, fanOut 10000, depth 512`, overridable per graph), `contractMode`, and an `invoke` entry point so a run started from a template carries its own execution id and names the revision the document is stamped with; `scheduler.instance.url(...)` now returns that id. Effects the browser cannot carry (`secret`, `storage:kv`) still run their capability check and then throw `EffectUnavailable` naming placement as the fix, so the answer points at the graph rather than at a missing global. |
| 2026-09-21 | PB-054 browser half: executions reported to the server | done, deployed | `POST /crdt/{id}/executions` (`src/runtime/ingest.ts`). A report is evidence from a client and is treated as such: identity, graph and `domain: "browser"` are stamped from the request, unknown kinds and stray fields are dropped, ids must be ULIDs, volume is capped at 5000 observations / 2 MiB (413 `LIMIT_EXCEEDED`), and the first report of an execution wins so a retry cannot rewrite it. The editor queues reports and sends them one at a time, keeps the last 200 executions of the session in the orchestrator store, drops the oldest beyond 50 queued, and never lets a failed report break the running graph. `observations.query` and the execution resources then answer for both domains with no change. |
| 2026-09-21 | PB-045 editor half | done | Connecting or moving a connector asks the shared `assignable(out, in)` rather than the old string comparison, and `schemaConflict(out, in)` says when two declared JSON Schemas will disagree once values flow (different JSON types, or required properties the producer's closed schema never emits). A type mismatch still refuses the connection; a schema conflict is shown while dragging and left to the person, because a schema can be wrong in ways only they can judge. |
| 2026-09-21 | Tests | 148 server, 90 editor CRDT specs, 17 integration, ratchet 460 | server `runtime.js` +3 (ingest: identity stamped over the body, unknown kinds and stray fields dropped, first report wins, admission/shape/volume refusals); editor `packages/GraphCrdt/__tests__/runtime.spec.ts` (9): capability parsing and scope matching, layer narrowing with the refusing layer named, assignability and schema conflicts, capture/redaction/fingerprint, scheduler events into an ordered stream stamped `browser`, foreign executions ignored and volume capped, host fetch allowed/refused/plain-http, `EffectUnavailable` for secrets and the shared store, monotonic ids. |
| 2026-09-21 | Browser verification of the browser half | **passed** | Editor at `localhost:8080` against the AWS dev stage, "Thoughtless Coral" open. `scheduler.instance.url("RichCrocodile", …)` returned execution `01M32J9QE3MET4RZC5JV3ZT2N3`; the orchestrator recorded it as `domain browser`, `revisionId 01M32HDS2QMBSPNPRSR5C5ZW0K` (the document's stamp), `hops 1`, `errors 1`, `effects {allowed: 2, denied: 1}`, 7 observations, owner = the signed-in human. `observations.query {executionId}` from the same page returned them from the server: `exec.begin`, `edge.input`, `effect net:https:www.githubstatus.com allowed`, `effect.denied net:https:example.org (instance)`, `effect storage:kv demo/last allowed`, `exec.error EffectUnavailable: storage:kv effects do not run in the browser; place this node on the server`, `exec.end`. `plastic://graph/{id}/executions` lists all three runs of that node together: two server executions and the browser one. S3: `executions/01M32J9QE3….json` with the server-stamped owner and domain, `observations/<g>/2026092118/01M32J9QE3….ndjson`. The same node, the same grants, two domains, one query. |

Next: PB-062/063 isolated-vm executor (layer on the Lambda nodejs:22 image), PB-070/071 hybrid `edge.deliver`, PB-106 first journey, PB-114 observation replay in the editor, PB-084 tasks with `proposal.simulate`.
